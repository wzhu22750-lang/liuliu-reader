

==================================================
十七、后台完整导入，不允许边下载边阅读
==================================================

这是一个非常重要的产品需求。

当前项目可能存在：

“下载一章 → 解码一章 → 用户马上阅读 → 后台继续下载”

这种边下载边阅读的模式。

请彻底取消这种行为。

新的产品逻辑必须是：

用户输入小说链接
↓
创建“导入任务”
↓
后台执行完整小说下载
↓
获取全部章节
↓
逐章获取完整正文
↓
逐章完成番茄字体解码
↓
验证每一章正文完整
↓
全部章节成功
↓
组装完整 Book / Chapter 数据
↓
生成 TXT
↓
导入书架
↓
任务完成
↓
用户才能打开阅读

也就是说：

【导入未完成 = 不允许阅读】

==================================================
十八、导入任务必须在后台执行
==================================================

小说下载、正文获取、字体解码、TXT 生成等耗时操作不能阻塞阅读器 UI。

应该建立后台 Import Task / Download Task。

例如：

ImportTask
{
    bookId
    title
    status
    totalChapters
    completedChapters
    currentChapter
    progress
    error
}

状态至少包括：

PENDING
DOWNLOADING
DECODING
EXPORTING
IMPORTING
COMPLETED
FAILED
CANCELLED

如果当前技术栈有现成的后台任务框架，优先使用现有框架。

不要为了这个需求重新引入不必要的依赖。

==================================================
十九、进度显示
==================================================

用户点击“导入小说”以后，不要让用户误以为已经可以阅读。

应该明确显示：

正在导入小说

《神通者》

正在获取章节……

已完成 35 / 300 章

████████░░░░░░░░░░

或者根据当前 UI 设计采用更合适的进度展示。

阶段应该尽量明确：

获取章节目录
↓
获取正文
↓
解码正文
↓
整理小说
↓
生成 TXT
↓
加入书架
↓
完成

最终：

“导入完成，可以开始阅读”

==================================================
二十、书架状态
==================================================

在导入过程中：

不要把一个“半成品 Book”直接放进正常书架并允许打开。

可以有两种实现：

方案 A：

导入过程中显示：

“正在导入”

但点击后不能进入 Reader。

方案 B：

只有 COMPLETED 状态的 Book 才正式加入书架。

优先采用方案 B。

也就是说：

数据库中的 Book 最好只有在完整导入成功之后才进入正式可阅读状态。

如果需要保存导入进度，可以使用单独的 ImportTask / DownloadTask 数据。

==================================================
二十一、严禁边解码边阅读
==================================================

不要实现：

chapter 1 下载完成
↓
chapter 1 解码完成
↓
加入 Book
↓
用户开始阅读 chapter 1
↓
chapter 2、3、4……继续后台下载

这是本次需求明确禁止的行为。

用户必须等待：

所有章节下载完成
+
所有章节正文完整
+
所有章节解码完成
+
TXT/Book 数据准备完成

之后才能阅读。

==================================================
二十二、解码必须在后台完成
==================================================

字体解码属于导入流程的一部分。

例如：

Chapter 1
↓
获取原始正文
↓
FanqieFontDecoder
↓
验证
↓
保存解码后的正文

然后继续：

Chapter 2
↓
获取
↓
解码
↓
验证

……

直到最后一章。

最终 Book 中保存的正文必须已经是：

正常 Unicode 中文

而不是：

PUA 字符

也不能要求 Reader 在打开章节时再进行字体解码。

也就是说：

Reader 层禁止承担番茄字体解码工作。

==================================================
二十三、阅读器必须只读取“已完成”的 Book
==================================================

Reader 打开小说时，不应该再：

- 请求番茄 API
- 下载章节
- 解码字体
- 等待网络
- 处理 PUA
- 拼接正文

Reader 的职责应该非常简单：

Book
↓
Chapter
↓
读取已经准备好的正文
↓
显示

这样可以保证阅读过程：

- 无网络也可以阅读
- 打开章节立即显示
- 不会突然出现乱码
- 不会出现半章
- 不会因为后台下载失败而影响阅读

==================================================
二十四、导入失败处理
==================================================

如果导入过程中：

第 153 / 300 章获取失败

不能把前 152 章作为“可阅读小说”加入书架。

应该：

ImportTask = FAILED

并显示：

“导入失败”

“第 153 章获取失败”

提供：

重试

如果实现了断点续传：

重试应该从失败章节继续，而不是从第一章重新开始。

==================================================
二十五、导入完成后的原子性
==================================================

最好做到：

“完整导入”是一个原子操作。

也就是说：

未完成：

Book 不可阅读

完成：

一次性将完整 Book 标记为：

READY / COMPLETED

例如：

IMPORTING
↓
所有章节验证通过
↓
TXT 导出成功
↓
Book 数据写入完成
↓
status = READY

只有：

status == READY

才能进入 Reader。

避免出现：

数据库里已经存在一本书，
但里面只有前 20 章，
用户却可以打开阅读。

==================================================
二十六、最终产品体验
==================================================

用户体验必须是：

1. 用户粘贴：

推荐一部好书《神通者》https://changdunovel.com/t/BTRdctuGVyI/

2. 点击：

“导入小说”

3. App 显示：

正在导入《神通者》

获取章节目录
300 章

4. 后台继续执行：

获取正文
→
完整性检查
→
字体解码
→
保存

5. 用户可以离开当前页面，导入任务继续执行。

6. 导入过程中：

书架中的小说不能进入正常阅读状态。

7. 全部完成：

“《神通者》导入完成”

8. 自动加入书架。

9. 用户点击小说。

10. 直接开始阅读完整小说。

整个过程：

下载、解码、导出、数据库写入

全部属于后台 Import Task。

Reader 不参与下载和解码。

==================================================
二十七、最终架构
==================================================

最终希望形成：

                 用户输入 URL
                       ↓
                  ImportTask
                       ↓
              ShareLinkResolver
                       ↓
                 BookResolver
                       ↓
             ChapterListFetcher
                       ↓
        ┌──────────────┴──────────────┐
        ↓                             ↓
 ChapterContentFetcher          Progress UI
        ↓
 Complete Content Validator
        ↓
 FanqieFontDecoder
        ↓
 Decoded Chapter
        ↓
     BookAssembler
        ↓
     TxtExporter
        ↓
   BookshelfRepository
        ↓
   status = READY
        ↓
       Reader

Reader 本身：

Reader
 ↓
BookshelfRepository
 ↓
READY Book
 ↓
Chapter
 ↓
content

Reader 不再负责：

下载
解码
网络请求
字体解析
章节补全

==================================================
二十八、最终验收标准增加
==================================================

以下任何一项不满足，都不算完成：

□ 可以通过小说分享链接创建导入任务
□ 导入任务在后台执行
□ UI 不被网络请求阻塞
□ 用户可以看到导入进度
□ 所有章节都必须获取
□ 第 1 章完整
□ 第 10 章完整
□ 第 11 章完整
□ 第 20 章完整
□ 第 50 章完整
□ 最后一章完整
□ 所有章节都完成字体解码
□ 不存在 PUA 字符
□ 不存在半章正文
□ 导入过程中不能阅读
□ Reader 不执行字体解码
□ Reader 不执行网络请求
□ 所有章节完成后才标记 Book = READY
□ READY 后自动进入书架
□ TXT 完整导出
□ TXT 使用 UTF-8
□ 导入失败可以重试
□ 不允许半成品进入可阅读书架