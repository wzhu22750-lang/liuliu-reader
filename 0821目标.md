请直接检查并修改当前项目，实现一个完整的“小说链接 → 完整 TXT → 自动导入书架”流程。

项目目标：

这是一个小说阅读器。用户最终只需要输入/粘贴一个小说分享链接，例如：

推荐一部好书《神通者》https://changdunovel.com/t/BTRdctuGVyI/

程序应该自动完成：

用户输入链接
→ 识别并提取 URL
→ 解析分享链接
→ 获取小说基本信息
→ 获取完整章节目录
→ 获取所有章节正文
→ 对番茄小说字体混淆进行解码
→ 组装完整小说
→ 导出 UTF-8 TXT
→ 自动导入本地书架
→ 用户可以直接阅读

重点：

目前项目已经解决了番茄小说字体混淆/PUA 字符解码问题。

现在真正的问题是：

现有借鉴的开源项目只能：

- 前 10 章：能够获取完整正文
- 第 11 章开始：每一章只能获取正文开头的一小部分

也就是说，目前不是“导出乱码”问题。

而是：

【章节正文获取不完整】

必须解决“10 章以后正文被截断”的问题。

==================================================
一、不要简单修补现有代码
==================================================

请先完整阅读当前仓库代码，以及当前项目借鉴的开源项目。

重点分析：

1. 小说链接解析
2. 小说 ID / book_id 获取
3. 章节列表获取
4. chapter_id 获取
5. 正文 API
6. 正文分页
7. 正文加密/混淆
8. 字体解码
9. TXT 导出
10. 书架导入

同时研究当前项目参考的开源实现。

如果仓库中已经存在相关代码，请先找出它实际上是如何获取前 10 章和第 11 章之后正文的。

不要凭经验猜。

==================================================
二、重点调查“第 10 章之后只有开头”的真正原因
==================================================

请把这个问题当成最高优先级。

目前表现：

第 1～10 章：

chapter content = 完整

第 11 章开始：

chapter content = 只有开头

例如：

第 11 章：
【章节标题】
正文开头……
然后突然结束。

这说明：

章节列表本身可能是正常的，

但正文获取过程存在问题。

请重点检查以下可能性：

A. 正文接口发生变化

前 10 章和后续章节使用了不同接口。

B. 正文存在分页

例如：

page=1
page=2
page=3

现有代码只请求了第一页。

C. 正文存在 cursor / offset / next_token

例如：

cursor
offset
next_cursor
next_page
has_more

现有代码没有继续请求。

D. API 返回正文长度有限制

可能需要多次请求才能获得完整内容。

E. 第 10 章是某个接口的边界

例如：

免费章节 / 预览章节 / 不同章节接口。

F. 后续章节使用不同的 content 字段

例如前 10 章：

data.content

后续：

data.paragraphs
data.sections
data.text

G. 后续章节需要不同的 chapter_id / item_id / group_id。

H. 现有代码错误地复用了前 10 章的请求参数。

I. 请求被服务端限制，导致返回 preview/snippet。

J. 正文实际上存在于 HTML / JSON / embedded data 中，但当前 parser 只取了其中一部分。

请通过实际请求、响应结构和已有开源项目代码来判断到底是哪一种。

==================================================
三、必须研究开源项目，而不是重新发明接口
==================================================

请搜索并研究当前项目借鉴的开源项目，以及 GitHub 上其他成熟的番茄小说下载器/解析器。

重点关注：

- 如何获取完整章节列表
- 如何获取章节正文
- 第 10 章之后如何处理
- 是否存在分页
- 是否存在不同正文接口
- 是否有 cursor / next token
- 是否根据章节类型选择不同 endpoint
- 是否需要额外请求章节详情
- 是否存在字体映射
- 如何处理接口变化

如果发现已有开源项目已经解决了：

“第 10 章之后正文不完整”

请优先借鉴其请求流程和数据结构。

但是：

不要直接复制整个项目。

只移植真正需要的逻辑。

==================================================
四、实现真正的“完整章节获取器”
==================================================

不要认为：

“API 返回了 content 字段”

就代表这一章已经完整。

必须建立完整性检查。

例如：

fetchChapter(chapter)

应该最终返回：

完整正文

而不是：

第一次请求返回什么就直接保存什么。

如果发现接口支持分页：

应该类似：

fetch page 1
↓
检查 has_more / next_cursor
↓
fetch page 2
↓
继续
↓
直到 has_more = false
↓
合并所有正文
↓
返回完整 chapter.content

不要写死：

只请求 10 章

也不要写死：

只请求第一页。

==================================================
五、增加“章节完整性验证”
==================================================

这是非常重要的。

每个章节获取后都应该进行基本验证。

例如：

- content 是否为空
- content 长度是否异常短
- 是否明显只有开头
- 是否出现 API 返回的 preview 标记
- 是否存在 next page / cursor
- 是否还有未获取内容

如果发现章节正文明显不完整：

不要直接导出。

应该：

1. 尝试继续请求
2. 如果存在分页，继续分页
3. 如果存在备用正文接口，尝试备用接口
4. 如果最终仍然无法获得完整正文，记录明确错误

不要把“半章内容”当作成功。

==================================================
六、不要用“固定长度”判断正文完整
==================================================

禁止这种简单逻辑：

if content.length < 1000:
    retry()

因为不同章节长度可能完全不同。

应该根据真实 API 的：

- has_more
- next_cursor
- page
- offset
- total
- content_length
- chapter metadata

或者参考开源项目的实际判断方式。

如果 API 没有明确分页字段，可以根据响应结构设计合理的完整性判断。

==================================================
七、完整小说下载流程
==================================================

最终应该实现：

Input URL
↓
extract URL
↓
resolve share URL
↓
get book info
↓
get chapter list
↓
for each chapter:
    get complete chapter content
    decode Fanqie font obfuscation
    validate content
↓
all chapters complete
↓
assemble TXT
↓
save UTF-8 TXT
↓
import into bookshelf
↓
open reader

要求：

“所有章节成功”之后才算下载成功。

如果第 11 章失败：

不能：

“前 10 章成功，所以导出一本只有前 10 章的书”。

应该明确告诉用户：

第 11 章获取失败。

==================================================
八、TXT 导出
==================================================

TXT 必须：

- UTF-8
- 中文正常
- 章节标题正常
- 换行正常
- 正文完整
- 不包含 PUA 字符
- 不包含 HTML 标签
- 不包含 API JSON
- 不包含调试信息

结构类似：

《神通者》

第一章 XXXXX

正文……

第二章 XXXXX

正文……

……

最后一章 XXXXX

正文……

==================================================
九、自动导入书架
==================================================

最终用户不应该还需要手动：

下载 TXT
→ 再选择 TXT
→ 再导入

而应该：

用户输入小说链接
↓
点击“导入”
↓
程序后台获取小说
↓
生成 TXT / 或直接生成内部 Book
↓
自动加入书架
↓
显示：

《神通者》
作者：XXX
章节：XXX章
已导入

用户点击即可阅读。

如果当前架构已经有 Book / Chapter 数据模型：

优先直接构造 Book + Chapter。

TXT 可以作为导出文件。

不要为了实现书架导入而：

先生成 TXT
→ 再重新解析 TXT
→ 再导入书架

除非当前项目架构已经如此设计。

最合理的是：

API
→ Book/Chapter model
→ BookShelf

同时：

Book/Chapter model
→ TXT exporter

==================================================
十、架构要求
==================================================

建议形成清晰的模块：

ShareLinkParser
    ↓
BookResolver
    ↓
ChapterListFetcher
    ↓
ChapterContentFetcher
    ↓
FanqieFontDecoder
    ↓
ChapterValidator
    ↓
BookAssembler
    ↓
BookRepository / Bookshelf
    ↓
TxtExporter

不要把所有逻辑塞进一个函数。

==================================================
十一、并发和限速
==================================================

如果小说有几百章：

不要一次性同时请求几百个章节。

应该采用：

- 有限并发
- 请求间隔
- retry
- exponential backoff
- 超时
- 单章节失败重试

例如：

并发 3～5 个章节

但具体数值请根据现有项目架构和服务端限制决定。

不要为了速度把请求打爆。

==================================================
十二、失败恢复
==================================================

如果下载 500 章小说：

第 300 章网络失败。

不要让前 299 章全部丢失。

应该允许：

- 重试当前章节
- 从失败章节继续
- 保存下载进度
- 重新导入时跳过已经成功的章节

如果当前项目架构暂时不适合实现完整断点续传，至少实现：

章节级 retry。

==================================================
十三、真实测试
==================================================

必须使用真实的：

https://changdunovel.com/t/BTRdctuGVyI/

进行测试。

不要只测试 mock 数据。

至少验证：

1. URL 能识别
2. 能解析小说
3. 能获得章节列表
4. 第 1 章完整
5. 第 10 章完整
6. 第 11 章完整
7. 第 20 章完整
8. 更后面的章节完整
9. 最终章节完整
10. TXT 完整
11. TXT 中文正常
12. 自动导入书架
13. 书架打开后可以正常阅读

尤其要比较：

第 1 章正文长度
第 10 章正文长度
第 11 章正文长度
第 20 章正文长度

确保不是：

前 10 章正常
↓
第 11 章以后全部只有 preview。

==================================================
十四、不要硬编码这一本书
==================================================

禁止：

if bookId == xxx

禁止：

if chapterIndex > 10

禁止：

if chapterId == xxx

禁止针对《神通者》写特殊逻辑。

必须实现：

通用小说链接 → 完整章节下载。

==================================================
十五、最终验收标准
==================================================

我的最终目标非常简单：

用户只需要：

粘贴：

推荐一部好书《神通者》https://changdunovel.com/t/BTRdctuGVyI/

点击：

“导入小说”

然后程序自动完成：

解析链接
↓
获取小说
↓
获取完整章节目录
↓
获取所有章节完整正文
↓
字体解码
↓
生成完整 TXT
↓
自动加入书架
↓
可以直接阅读

整个过程不应该需要用户：

- 手动复制 book ID
- 手动复制章节
- 手动下载 TXT
- 手动导入 TXT
- 手动处理字体
- 手动处理乱码

==================================================
十六、修改前先给出诊断
==================================================

在开始大规模修改前，请先告诉我：

1. 当前仓库的小说下载流程是什么？
2. 第 1～10 章为什么完整？
3. 第 11 章之后为什么只有开头？
4. 参考开源项目中正确的完整正文获取方式是什么？
5. 当前代码应该修改哪些模块？

然后再直接实施修改。

不要停留在分析阶段。

完成后：

- 修改代码
- 运行测试
- 构建项目
- 使用真实链接测试
- 报告测试结果
- 如果真实 API 因网络环境无法访问，请明确说明具体卡点，不要伪造测试成功。

==================================================
十七、后台完整导入，不允许边下载边阅读
==================================================

这是一个非常重要的产品需求。

当前项目可能存在：

“下载一章 → 解码一章 → 用户马上阅读 → 后台继续下载”

这种边下载边阅读的模式。

请彻底取消这种行为。

新的产品逻辑必须是：

用户输入小说链接
↓
创建“导入任务”
↓
后台执行完整小说下载
↓
获取全部章节
↓
逐章获取完整正文
↓
逐章完成番茄字体解码
↓
验证每一章正文完整
↓
全部章节成功
↓
组装完整 Book / Chapter 数据
↓
生成 TXT
↓
导入书架
↓
任务完成
↓
用户才能打开阅读

也就是说：

【导入未完成 = 不允许阅读】

==================================================
十八、导入任务必须在后台执行
==================================================

小说下载、正文获取、字体解码、TXT 生成等耗时操作不能阻塞阅读器 UI。

应该建立后台 Import Task / Download Task。

例如：

ImportTask
{
    bookId
    title
    status
    totalChapters
    completedChapters
    currentChapter
    progress
    error
}

状态至少包括：

PENDING
DOWNLOADING
DECODING
EXPORTING
IMPORTING
COMPLETED
FAILED
CANCELLED

如果当前技术栈有现成的后台任务框架，优先使用现有框架。

不要为了这个需求重新引入不必要的依赖。

==================================================
十九、进度显示
==================================================

用户点击“导入小说”以后，不要让用户误以为已经可以阅读。

应该明确显示：

正在导入小说

《神通者》

正在获取章节……

已完成 35 / 300 章

████████░░░░░░░░░░

或者根据当前 UI 设计采用更合适的进度展示。

阶段应该尽量明确：

获取章节目录
↓
获取正文
↓
解码正文
↓
整理小说
↓
生成 TXT
↓
加入书架
↓
完成

最终：

“导入完成，可以开始阅读”

==================================================
二十、书架状态
==================================================

在导入过程中：

不要把一个“半成品 Book”直接放进正常书架并允许打开。

可以有两种实现：

方案 A：

导入过程中显示：

“正在导入”

但点击后不能进入 Reader。

方案 B：

只有 COMPLETED 状态的 Book 才正式加入书架。

优先采用方案 B。

也就是说：

数据库中的 Book 最好只有在完整导入成功之后才进入正式可阅读状态。

如果需要保存导入进度，可以使用单独的 ImportTask / DownloadTask 数据。

==================================================
二十一、严禁边解码边阅读
==================================================

不要实现：

chapter 1 下载完成
↓
chapter 1 解码完成
↓
加入 Book
↓
用户开始阅读 chapter 1
↓
chapter 2、3、4……继续后台下载

这是本次需求明确禁止的行为。

用户必须等待：

所有章节下载完成
+
所有章节正文完整
+
所有章节解码完成
+
TXT/Book 数据准备完成

之后才能阅读。

==================================================
二十二、解码必须在后台完成
==================================================

字体解码属于导入流程的一部分。

例如：

Chapter 1
↓
获取原始正文
↓
FanqieFontDecoder
↓
验证
↓
保存解码后的正文

然后继续：

Chapter 2
↓
获取
↓
解码
↓
验证

……

直到最后一章。

最终 Book 中保存的正文必须已经是：

正常 Unicode 中文

而不是：

PUA 字符

也不能要求 Reader 在打开章节时再进行字体解码。

也就是说：

Reader 层禁止承担番茄字体解码工作。

==================================================
二十三、阅读器必须只读取“已完成”的 Book
==================================================

Reader 打开小说时，不应该再：

- 请求番茄 API
- 下载章节
- 解码字体
- 等待网络
- 处理 PUA
- 拼接正文

Reader 的职责应该非常简单：

Book
↓
Chapter
↓
读取已经准备好的正文
↓
显示

这样可以保证阅读过程：

- 无网络也可以阅读
- 打开章节立即显示
- 不会突然出现乱码
- 不会出现半章
- 不会因为后台下载失败而影响阅读

==================================================
二十四、导入失败处理
==================================================

如果导入过程中：

第 153 / 300 章获取失败

不能把前 152 章作为“可阅读小说”加入书架。

应该：

ImportTask = FAILED

并显示：

“导入失败”

“第 153 章获取失败”

提供：

重试

如果实现了断点续传：

重试应该从失败章节继续，而不是从第一章重新开始。

==================================================
二十五、导入完成后的原子性
==================================================

最好做到：

“完整导入”是一个原子操作。

也就是说：

未完成：

Book 不可阅读

完成：

一次性将完整 Book 标记为：

READY / COMPLETED

例如：

IMPORTING
↓
所有章节验证通过
↓
TXT 导出成功
↓
Book 数据写入完成
↓
status = READY

只有：

status == READY

才能进入 Reader。

避免出现：

数据库里已经存在一本书，
但里面只有前 20 章，
用户却可以打开阅读。

==================================================
二十六、最终产品体验
==================================================

用户体验必须是：

1. 用户粘贴：

推荐一部好书《神通者》https://changdunovel.com/t/BTRdctuGVyI/

2. 点击：

“导入小说”

3. App 显示：

正在导入《神通者》

获取章节目录
300 章

4. 后台继续执行：

获取正文
→
完整性检查
→
字体解码
→
保存

5. 用户可以离开当前页面，导入任务继续执行。

6. 导入过程中：

书架中的小说不能进入正常阅读状态。

7. 全部完成：

“《神通者》导入完成”

8. 自动加入书架。

9. 用户点击小说。

10. 直接开始阅读完整小说。

整个过程：

下载、解码、导出、数据库写入

全部属于后台 Import Task。

Reader 不参与下载和解码。

==================================================
二十七、最终架构
==================================================

最终希望形成：

                 用户输入 URL
                       ↓
                  ImportTask
                       ↓
              ShareLinkResolver
                       ↓
                 BookResolver
                       ↓
             ChapterListFetcher
                       ↓
        ┌──────────────┴──────────────┐
        ↓                             ↓
 ChapterContentFetcher          Progress UI
        ↓
 Complete Content Validator
        ↓
 FanqieFontDecoder
        ↓
 Decoded Chapter
        ↓
     BookAssembler
        ↓
     TxtExporter
        ↓
   BookshelfRepository
        ↓
   status = READY
        ↓
       Reader

Reader 本身：

Reader
 ↓
BookshelfRepository
 ↓
READY Book
 ↓
Chapter
 ↓
content

Reader 不再负责：

下载
解码
网络请求
字体解析
章节补全

==================================================
二十八、最终验收标准增加
==================================================

以下任何一项不满足，都不算完成：

□ 可以通过小说分享链接创建导入任务
□ 导入任务在后台执行
□ UI 不被网络请求阻塞
□ 用户可以看到导入进度
□ 所有章节都必须获取
□ 第 1 章完整
□ 第 10 章完整
□ 第 11 章完整
□ 第 20 章完整
□ 第 50 章完整
□ 最后一章完整
□ 所有章节都完成字体解码
□ 不存在 PUA 字符
□ 不存在半章正文
□ 导入过程中不能阅读
□ Reader 不执行字体解码
□ Reader 不执行网络请求
□ 所有章节完成后才标记 Book = READY
□ READY 后自动进入书架
□ TXT 完整导出
□ TXT 使用 UTF-8
□ 导入失败可以重试
□ 不允许半成品进入可阅读书架