---

# Codex Task Specification: 番茄小说完整下载与书架导入系统改造

## 1. 核心目标 (Mission Objective)
重构并完善当前项目的小说导入流程，支持用户仅需输入小说分享链接（如 `https://changdunovel.com/t/BTRdctuGVyI/`），即可在后台完整抓取全本正文（**彻底解决第11章及之后正文被截断/只有开头的问题**）、自动完成番茄字体混淆解码、生成 UTF-8 TXT 并原子性入库书架，实现“导入完成前禁止阅读、导入完成后即点即读、Reader 零网络/零解码”的高可用架构。

---

## 2. Phase 1: 必须先进行的诊断 (Investigation & Root Cause Analysis)
在修改代码前，必须先定位当前仓库的实现并给出具体分析结论：
1. **现有流程排查**：分析当前项目获取前 10 章与第 11 章之后正文的代码路径、请求 API 及请求参数。
2. **10章截断根因定位**：
   - 是否使用了 Web 端未登录/匿名预览接口（如 `fanqienovel.com/api/reader/full` 在 10 章后转为 preview 模式）？
   - 是否缺少移动端客户端特定请求头/参数（如 `novel.snssdk.com` 或 `api.cengui.cn` 等全本接口所需的 `device_platform=android`, `version_code`, `aid`, `iid` 等）？
   - 是否存在正文分页/分段字段（`cursor` / `next_cursor` / `has_more` / `paragraphs`）？
3. **开源方案借鉴**：参考成熟开源项目（如 GitHub 上番茄小说解析器/书源规则）中对全本章节（含 10 章以后）的稳定 API 调用方式，制定修复策略。

---

## 3. Phase 2: 核心产品与业务逻辑规范

### 3.1 彻底废除“边下/边解边读”，引入后台原子导入任务
- **禁止行为**：严禁“下载一章 -> 解码一章 -> 立即允许阅读 -> 后台继续下载”。
- **原子性入库**：只有全部章节下载完成、全部正文解码验证通过、TXT 导出就绪后，书籍状态才变更为 `READY`，正式加入可阅读书架。
- **未完成状态隔离**：导入中或导入失败的书籍不可进入 Reader，数据库/UI 必须严格区分未就绪与已就绪状态。

### 3.2 任务状态机设计 (ImportTask)
实现明确的任务状态机与进度通知：
- **状态流转**：`PENDING` ➔ `RESOLVING` ➔ `FETCHING_CHAPTERS` ➔ `DOWNLOADING_CONTENT` ➔ `DECODING` ➔ `EXPORTING_TXT` ➔ `SAVING_SHELF` ➔ `COMPLETED`（或 `FAILED` / `CANCELLED`）。
- **进度粒度**：向 UI 上报实时阶段（如 `获取目录 (共300章)` ➔ `下载正文 (35/300)` ➔ `字体解码` ➔ `导出TXT并入库`）。
- **失败恢复**：若第 N 章网络失败，支持章节级重试（Retry）与指数退避（Exponential Backoff），不可因单章失败导致全本数据丢失或导入半成品。

### 3.3 阅读器 (Reader) 彻底解耦
- **Reader 零开销**：Reader 仅负责从本地数据库/文件系统读取已完成解码的纯文本 Unicode 字符并渲染。
- **严禁在 Reader 层执行**：网络请求、番茄 PUA 字体解码、章节内容拼接补全。

---

## 4. Phase 3: 架构重构与模块划分 (Architecture Specification)

请将导入流程拆分为清晰解耦的模块：

```
                   用户输入文本 (包含小说分享链接)
                                 │
                                 ▼
                     ┌───────────────────────┐
                     │   ShareLinkParser     │ 提取 URL 并解析 302 重定向/获取 book_id
                     └───────────┬───────────┘
                                 │
                                 ▼
                     ┌───────────────────────┐
                     │      BookResolver     │ 获取小说元数据 (书名/作者/封面/简介)
                     └───────────┬───────────┘
                                 │
                                 ▼
                     ┌───────────────────────┐
                     │  ChapterListFetcher   │ 获取完整章节目录列表
                     └───────────┬───────────┘
                                 │
                                 ▼
                     ┌───────────────────────┐
                     │ ChapterContentFetcher │ 稳定正文获取 (并发控制 3~5 + 限速 + 分页处理)
                     └───────────┬───────────┘
                                 │
                                 ▼
                     ┌───────────────────────┐
                     │   FanqieFontDecoder   │ 番茄小说 PUA 字体混淆后台解码 (转为标准 Unicode)
                     └───────────┬───────────┘
                                 │
                                 ▼
                     ┌───────────────────────┐
                     │   ChapterValidator    │ 完整性验证 (检查 has_more/非空/无截断/无 PUA 残留)
                     └───────────┬───────────┘
                                 │
                   ┌─────────────┴─────────────┐
                   ▼                           ▼
       ┌───────────────────────┐   ┌───────────────────────┐
       │      TxtExporter      │   │  BookshelfRepository  │
       │ (导出标准 UTF-8 TXT)   │   │  (落库 Book/Chapter)   │
       └───────────────────────┘   └───────────┬───────────┘
                                               │ (status = READY)
                                               ▼
                                   ┌───────────────────────┐
                                   │        Reader         │
                                   │  (纯本地快速渲染，无网络) │
                                   └───────────────────────┘
```

---

## 5. Phase 4: 质量与完整性控制 (Robustness & Integrity)

1. **正文完整性判断 (ChapterValidator)**：
   - 严禁使用硬编码字符长度（如 `length < 1000`）判断完整性。
   - 依据 API 的 `has_more`、`total`、`paragraph count`、或成熟开源项目的结构特征进行完整性校验。
   - 确保第 10 章之后的正文不是预览片段（Preview/Snippet）。
2. **字体与文本净化**：
   - 导出与落库的文本必须是纯净中文，彻底清除 HTML 标签、PUA 私有区字符、API 冗余 JSON、调试信息。
3. **通用性与代码洁癖**：
   - 严禁硬编码任何特定的 `book_id`、`chapter_id`、`chapterIndex > 10` 或针对特定书名的特判逻辑。

---

## 6. Phase 5: 真实测试与最终验收标准 (Acceptance Criteria)

### 测试用例：
使用真实链接 `推荐一部好书《神通者》https://changdunovel.com/t/BTRdctuGVyI/` 进行端到端测试。

### 验收清单：
- [ ] **链接解析**：输入包含文字的混合分享串，能准确提取 URL 并解析出有效的小说信息。
- [ ] **目录获取**：完整拉取全书目录，章节无遗漏、顺序正确。
- [ ] **正文长度对比验证**：
  - 验证第 1 章、第 10 章、第 11 章、第 20 章、第 50 章及末尾章节的正文长度与完整度，**确保第 11 章及后续章节内容完整，无截断**。
- [ ] **字体解码**：所有章节的正文均在后台完成解码，落库及 TXT 文件中无 PUA 乱码。
- [ ] **异步原子导入**：导入过程中 Reader 无法打开该书；全书成功后一次性变为 `READY` 并自动入库书架。
- [ ] **TXT 导出验证**：导出 UTF-8 编码的 TXT 文件，排版工整，包含书名与各章标题。
- [ ] **Reader 即点即读**：书架点击已完成的书籍，秒级打开阅读，阅读期间无任何网络请求与解码计算。
- [ ] **环境限制说明**：若在特定网络环境下遇到 API 访问受限（如 IP 限制/验证码），应详细记录阻断点与响应报文，不得伪造测试通过。